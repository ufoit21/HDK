<%= form_for(@assigned_project) do |f| %>
  <% if @assigned_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assigned_project.errors.count, "error") %> prohibited this assigned_project from being saved:</h2>

      <ul>
      <% @assigned_project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.collection_select(:user_id, User.all,:id,:email) %>
  </div>
  <div class="field">
    <%= f.label :project_id %><br />
    <%= f.collection_select(:project_id, Project.all,:id,:name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
